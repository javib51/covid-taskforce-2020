class Item {
  String photo;
  String name;
  int units;
  double price;
  bool available;
  String measureType;

  Item({this.photo, this.name, this.price, this.available, this.measureType, this.units});
}
